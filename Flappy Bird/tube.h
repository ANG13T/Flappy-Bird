#pragma once
#include <SFML/Graphics.hpp>
using namespace sf;
using namespace std;
/*
class toptube {
	public:
		Texture toptubeTexture;
		Sprite toptube;
		float yPos;
};


class bottomtube {
	public:
		Texture bottomtubeTexture;
		Sprite bottomTube;
		float yPos;
};

toptube();
bottomtube();

toptube(Vector2f position, Texture *toptubeTexture);
bottomtube(Vector2f position, Texture *bottomtubeTexture);

void Update();
void reposition();
void randomizeYPos();*/

class Tube {
public:
	Tube();
};